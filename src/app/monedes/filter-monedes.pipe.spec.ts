import { FilterMonedesPipe } from './filter-monedes.pipe';

describe('FilterMonedesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMonedesPipe();
    expect(pipe).toBeTruthy();
  });
});
